import React from "react";

function ComponentA() {
  return (
    <div>
      <h1>This is a Component</h1>
    </div>
  );
}

export default ComponentA;

//two ways to export: default and named

// for a file there can be only one default export
// but it can have n number of named export
